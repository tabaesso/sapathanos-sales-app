import React from 'react';
import { View, Text } from 'react-native';

export default function Account() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Conta do cliente</Text>
        </View>
    );
}